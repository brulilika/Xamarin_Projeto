<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Projeto.View.Popup.RegistroPopupPage"
             xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup">
    
    <StackLayout Margin="12"
                Padding="24"
                BackgroundColor="#82BAFA"
                HorizontalOptions="FillAndExpand"
                VerticalOptions="Center">

        <Entry Placeholder="Titulo" Text="{Binding Titulo}"/>
        <Entry Placeholder="Ocorrência" Text="{Binding Ocorrencia}"/>

        <Button Text="Registrar" BackgroundColor="#C2F5BC" Margin="5" CornerRadius="10" Command="{Binding RegistroCommand}"/>
    </StackLayout>
</pages:PopupPage>